# 2026-05-12

## TASK-015 · Status output polish

`vibeops status` 출력의 Notion 섹션이 더 이상 사용하지 않는 환경변수(`NOTION_API_KEY` / `NOTION_PROJECT_DB` / `NOTION_TASK_DB`)를 노출하던 문제와, GitHub/Package 상태가 status 명령에서 빠져 있어 사용자가 `vibeops github status` / `cat package.json` 을 따로 확인해야 했던 UX 결함을 함께 해결.

### 변경 사항

- `src/types/config.ts`
  - `NotionStatusSnapshot` (enabled / hasToken / tokenSource / hasProjectsTarget / hasTasksTarget) 추가.
  - `NotionTokenSource = ".vibeops.env" | "process.env" | "none"` 추가.
  - `GithubStatusSnapshot` (enabled / mode / owner / repo / remote / url) 추가.
  - `PackageStatusSnapshot` (exists / name / version / bin) 추가.
  - 기존 `NotionEnvSnapshot` 제거 (status 외부 미사용 확인 후).
- `src/lib/notion-env.ts`
  - `NotionTokenSource` 재export.
  - `getNotionTokenSource(cwd)` 추가 — `loadNotionEnv` 결과에서 토큰 원문은 떨어내고 `{ hasToken, source }` 만 반환. 호출자가 토큰을 실수로 로깅하지 못하도록 차단.
- `src/lib/config.ts`
  - `readNotionEnvSnapshot` 함수 + `NotionEnvSnapshot` import 제거. 외부 호출자 0건 확인.
- `src/status/collector.ts`
  - `readNotionEnvSnapshot` 호출 제거.
  - `snapshotNotion(config.notion, tokenProbe)` / `snapshotGithub(config.github)` / `snapshotPackage(readPackageJson result)` 로 분리.
  - Notion target 존재 여부는 `projectsTargetId` ∪ `projectsDatabaseId`, `tasksTargetId` ∪ `tasksDatabaseId` 로 판단해 legacy DB id 만 채운 사용자도 false 로 떨어지지 않게 함.
  - `package.json` 없는 디렉터리에서 `readPackageJson` 이 `null` 을 반환하면 `exists: false` 로 매핑.
  - `bin` 추출은 string (basename + 확장자 제거) / object (첫 키) / 미설정 ("") 세 경로 처리.
- `src/status/format.ts`
  - Notion 섹션 재설계: `enabled` / `token` / `projects target` / `tasks target` / `hint` 5줄. 토큰 줄은 `configured (.vibeops.env)` / `configured (process.env)` / `missing` 셋 중 하나. 토큰 원문 출력 0건.
  - GitHub 섹션 신규: 미설정 시 `enabled no` + `hint run \`vibeops github init\`` 두 줄, 설정 시 enabled/mode/owner repo/remote/url 모두 표시.
  - Package 섹션 신규: 없으면 `package.json missing` 한 줄, 있으면 name/version/bin.
  - label width 정렬용 `pad(label, width)` 헬퍼 도입.
  - `toJson` 에 `notion` (snapshot) / `github` (snapshot) / `package` (snapshot) 추가. 기존 키 (`cwd / isVibeopsProject / config / installation / tasks / git`) 는 그대로 유지.
- `README.md`
  - "Init Git Bootstrap" 직후에 **Status Output** 섹션 신규 추가. 풀 설정 / 미설정 / `package.json` 없는 케이스 세 가지 출력 예시 + JSON 노출 키 안내 + legacy 키 제거 정책 명시.
- `docs/project/03-current-state.md`
  - 현재 단계 줄에 "Status output polish" 추가.
  - TASK 목록 범위 `TASK-001 ~ TASK-015` 로 확장.
  - "Status 수집·포맷" 표 항목을 TASK-015 내용으로 보강.
- `docs/tasks/TASK-015-status-output-polish.md`
  - 새 TASK 파일 생성 (Status=Review, Goal / Scope / Out of Scope / Acceptance Criteria / Files to Inspect / Expected Files / Risks / Test Plan / Result / Test Result / Review Notes 18 섹션 형태).

### 검증

- `pnpm typecheck` ✓ — 신규 타입 / collector / formatter 모두 통과.
- `pnpm build` ✓.
- `pnpm smoke` ✓ — `--help`, `init --dry-run`, `init --dry-run --git --initial-commit`, `status`, `task generate --dry-run`, `notion init --dry-run`, `github status`, `github init --dry-run --connect goodtek/vibeops` 8 케이스 통과.
- `node dist/cli.js status` (vibeops 저장소): Notion `enabled no / token configured (.vibeops.env) / projects+tasks target missing / hint run \`vibeops notion init\``. GitHub `enabled yes / mode gh-cli / owner/repo goodtekxyz/vibeops / remote origin / url https://github.com/goodtekxyz/vibeops`. Package `name vibeops / version 0.1.0 / bin vibeops`. legacy 키 출력 0건.
- `node dist/cli.js status --json` (vibeops 저장소): `notion.tokenSource = ".vibeops.env"`, `hasToken true`, `hasProjectsTarget false`, `hasTasksTarget false`. `github.{enabled,mode,owner,repo,remote,url}` 모두 노출. `package.{exists,name,version,bin}` 노출.
- `/tmp/vibeops-task015-sandbox` (fresh `init --git --initial-commit`): Notion `enabled no / token missing / projects+tasks target missing / hint`. GitHub `enabled no / hint`. Package `package.json missing`. legacy 키 출력 0건 — `grep -E 'NOTION_API_KEY|NOTION_PROJECT_DB|NOTION_TASK_DB'` 0건.
- `/tmp/vibeops-task015-unborn` (fresh `init --git --no-initial-commit`): Git `branch main (unborn, no commits yet) / status dirty / hint create the first commit or run \`vibeops init --git --initial-commit\`` 유지. JSON `git.state = "unborn"`, `git.hasCommits = false`.
- `NOTION_TOKEN=secret_test_value node dist/cli.js status` (sandbox): token 줄이 `configured (process.env)` 로 전환. 토큰 원문 출력 0건.
- 정적 확인: `src/status/` 디렉터리 안에 `github-cli` / `notion-client` import 0건. status 가 `gh` 자식 프로세스 / Notion API 를 호출하지 않음.

### 남은 위험 요소

- (TASK-015 직후 발견된) `.vibeops.env.example` 와 `installer.ts` 의 legacy 키 잔재는 같은 날짜의 TASK-016 으로 처리 (아래 항목).
- `package.json` 의 `bin` 이 객체 형태일 때 첫 키만 보여 줌. VibeOps 자기 자신처럼 단일 bin 만 가정한 단순화. 다중 bin 프로젝트에서는 향후 보강 필요.
- JSON 출력 스키마에 `notion` / `github` / `package` 세 객체가 새로 들어가 외부 자동화가 이 출력을 grep / jq 로 사용 중이면 영향이 갈 수 있음. 기존 키 (`cwd / isVibeopsProject / config / installation / tasks / git`) 는 그대로 유지했으므로 신규 키 추가만 일어남.

## TASK-016 · Notion env template cleanup

TASK-015 직후 발견된 후속 정리. 새 프로젝트가 `vibeops init` 으로 받는 `.vibeops.env.example` 과 template 문서(`docs/project/08-env.md`, `.vibeops/workflows/notion-sync.md`)가 여전히 legacy 환경변수를 안내하던 문제를 해결.

### 변경 사항

- `src/bootstrap/installer.ts`
  - `envExampleContents()` 가 `NOTION_TOKEN=` 한 줄 + Notion integration URL 헤더만 출력하도록 단순화. legacy 3종(`NOTION_API_KEY` / `NOTION_PROJECT_DB` / `NOTION_TASK_DB`) 제거. `GITHUB_TOKEN` / `OPENAI_*` 같은 다른 키 기본 시드 없음.
- `.vibeops.env.example` (vibeops 자체)
  - 새 install 결과와 1:1 일치하도록 재작성.
- `templates/docs/project/08-env.md`
  - env 표를 `NOTION_TOKEN` 한 줄로 축소. target ID 저장 위치(`.vibeops.json` `notion.{projectsTargetId,tasksTargetId}`)를 본문에 명시. legacy 키는 `>` 인용으로 deprecation 안내만 유지.
- `docs/project/08-env.md` (vibeops 자체) — template 과 동일 내용으로 sync.
- `templates/.vibeops/workflows/notion-sync.md`
  - 설정 블록을 `NOTION_TOKEN` 입력 안내 + target ID 저장 위치 안내로 갱신. legacy 키 인용은 deprecation 한 줄.
- `.vibeops/workflows/notion-sync.md` (vibeops 자체) — template 과 sync.
- `README.md`
  - Notion Setup 섹션에 (a) `NOTION_TOKEN` 이 유일한 env, (b) target ID 는 `.vibeops.json` 에 있음, (c) legacy 키 3종은 더 이상 사용 안 함 + 새 install 결과는 `NOTION_TOKEN=` 만 있음 — 세 줄 추가.
- `docs/project/03-current-state.md` — 현재 단계 / TASK 범위 / Bootstrap 엔진 표 항목 갱신.
- `docs/tasks/TASK-016-notion-env-template-cleanup.md` — 신규 18 섹션 TASK 파일 (Status=Review).

### 검증

- `pnpm typecheck` ✓ / `pnpm build` ✓ / `pnpm smoke` ✓ (8 케이스, 회귀 없음).
- `/tmp/vibeops-task016-sandbox` 에서 `node dist/cli.js init --git --initial-commit`:
  - 생성된 `.vibeops.env.example` 본문 = 헤더 주석 6줄 + 빈 줄 + `NOTION_TOKEN=` + 마지막 개행.
  - `grep -E '^NOTION_' .vibeops.env.example` → `NOTION_TOKEN=` 한 줄.
  - sandbox 의 `.vibeops/workflows/notion-sync.md` / `docs/project/08-env.md` 에는 legacy 키 raw 사용 없음 (인용 deprecation 안내 한 줄씩만 남음).
- 같은 sandbox 에서 `vibeops status` Notion 섹션은 TASK-015 와 동일하게 `enabled no / token missing / projects+tasks target missing / hint` — 회귀 없음.
- 저장소 install path (`templates/`, `src/bootstrap/installer.ts`, `.vibeops.env.example`, `docs/project/08-env.md`, `.vibeops/workflows/notion-sync.md`) 정적 grep: deprecation 안내(영문/한글) 외 raw legacy 키 사용 0건.
- 실제 `.vibeops.env` 파일은 새로 만들거나 커밋하지 않았고, 어떤 출력에도 `NOTION_TOKEN` 원문이 등장하지 않음.

### 남은 위험 요소

- vibeops 자체의 `docs/project/01-architecture.md` / `02-tech-stack.md` 는 2026-05-11 1차 설계 단계의 historical record 라서 본 TASK 범위 밖. 최신 사실은 `03-current-state.md` 가 책임지는 정책 유지.
- 기존 사용자의 `.vibeops.env` 에 legacy 키가 살아 있어도 VibeOps 는 무시한다. 자동 정리 도구는 만들지 않음. 안내만 제공.
- 헤더 주석에 Notion integration URL 을 박아 둠 — URL 변경 시 `installer.ts` 와 `.vibeops.env.example` 두 군데를 함께 갱신해야 함.

## TASK-017 · Public release polish

같은 날(2026-05-12) 사용자가 추가로 "TASK-016 · Public release polish" 명목으로 요청. 직전 TASK-016 (Notion env template cleanup) 과 ID 충돌이 있어 정책상 다음 번호인 **TASK-017** 로 진행. 목표는 VibeOps 를 실제 npm 공개 배포 가능한 오픈소스 CLI 로 마무리하는 것 — npm package 이름 변경 + README/CHANGELOG 공개 톤 정리 + CLI 출력 영문 통일 + support contacts 추가. 실제 publish 는 수행하지 않고 dry-run 까지만 검증.

### 변경 사항

- `package.json`
  - `name` 을 `vibeops` → `@goodtekxyz/vibeops` 로 변경.
  - `version` 을 `0.1.0` → `0.2.0`.
  - `publishConfig: { access: "public" }` 추가 — scoped package 가 기본 private 으로 게시되는 함정 차단.
  - `bin / repository / homepage / bugs / author / license / files / engines / scripts / dependencies / devDependencies` 모두 기존 그대로 유지.
- `CHANGELOG.md`
  - 새 `## [0.2.0] - 2026-05-12` 엔트리 추가: Highlights(공개 배포 준비) / Added(scoped package + publishConfig + support 섹션) / Changed(영문 통일 + Acme Automator 예시) / Removed(BYOBrowser / MVP 라벨 / 내부 단계 표현) / Verification(typecheck/build/smoke/publish dry-run).
  - 기존 `## [0.1.0] - 2026-05-11` 엔트리에서 `MVP 1 / 2 / 3 / 4` 단계 라벨 전부 제거하고 기능 묶음(Project bootstrap / Plan / Task generation / Git task lifecycle / Notion dashboard sync / Packaging) 톤으로 재작성.
- `README.md`
  - 풀 rewrite: "BYOBrowser Example Flow" → "Quick Tutorial: Acme Automator" 로 교체. 모든 BYOBrowser 잔재 제거.
  - "MVP Features" → "Features". 내부 MVP1~4 라벨, "not implemented in the MVP", "future maybe", "post-MVP" 등 내부 개발 흐름 표현 정리.
  - install 안내 `npm install -g vibeops` → `npm install -g @goodtekxyz/vibeops`.
  - 새 "Support" 섹션 추가: bugs / setup issues / usage questions → `support@goodtek.xyz`, collaboration / feedback → `hello@goodtek.xyz`, issue tracker 링크.
  - Notion 섹션에 "`MVP Phase` here is a Notion property name kept for compatibility … free-form label column" 한 줄 추가해 호환성과 README 톤을 양립.
  - Status output 예시도 `@goodtekxyz/vibeops 0.2.0` 으로 업데이트.
- CLI 출력 영문 통일 (사용자 노출 문자열만, 코드 주석은 보존):
  - `src/cli.ts` — program/`init`/`status`/`plan`/`agent {list,show,prompt}`/`task {generate,start,prompt,check,done,rollback,pull}`/`notion {init,test,sync}`/`github {status,init}` description 전부 영문. `(MVP 1)` / `(TASK-010)` / `(post-MVP 4)` 같은 내부 라벨 제거.
  - `src/commands/notion-init.ts`, `notion-test.ts`, `notion-sync.ts`, `github-init.ts`, `github-status.ts`, `plan.ts`, `task-pull.ts`, `task-done.ts`, `task-rollback.ts`, `task-generate.ts` — 모든 `log.*` / `throw new Error(...)` / 가이드 메시지를 영문화.
  - `src/lib/brief.ts`, `prompt-builder.ts`, `task-generator.ts`, `task-summary.ts`, `task-scaffold.ts`, `notion-schema.ts`, `notion-sync.ts`, `notion-target.ts`, `inquirer-helpers.ts` — 사용자 노출 문자열, brief markdown 헤더, 생성된 TASK markdown 의 placeholder(`(not yet)` / `(unassigned)`), Notion 스키마 description, planning prompt 본문 텍스트 모두 영문 통일.
  - `src/agent/prompt.ts` — `FOOTER` 영문화 (Role/Inputs/Output Format/Rules/Forbidden + completion report 안내).
- 호환성 유지:
  - `MVP Phase` 는 Notion DB property 이름이므로 그대로 유지. README/Schema/Sync/Status/Template 모두 `MVP Phase` / `MVP <n>` 표기 유지하고 README 에서 "free-form select; compatibility name" 으로 명시.
  - `src/lib/task.ts` 의 한국어 placeholder regex (`/^\(.*미수행.*\)$/`) 와 status prefix regex (`/(신규|갱신|new|update|update:|신규:|갱신:)/`) 는 유지하되 영문 패턴(`new` / `update`)을 함께 인식하도록 둠. 기존 한국어 TASK markdown 도 새 CLI 로 정상 해석.
- `src/types/config.ts`
  - `BYOBrowser-style scaffolded directory` 주석을 `a scaffolded directory that has not adopted Node tooling yet` 로 일반화.
- `templates/.vibeops/agents/planner.md` / `.vibeops/agents/planner.md`
  - `BYOBrowser` 예시 한 줄 → `Acme Automator` 예시로 교체. 다른 템플릿 한국어 markdown 본문은 Out of Scope 정책대로 그대로 둠.
- `docs/project/03-current-state.md`
  - 현재 단계 줄에 "Public release polish" 추가, TASK 범위 `TASK-001 ~ TASK-017` 로 확장, "CLI 패키지 골격" / "CLI 진입점" 표 항목을 TASK-017 내용으로 보강.
- `docs/tasks/TASK-017-public-release-polish.md`
  - 새 18-section TASK 파일 (Status=Review, Goal / Background / Scope / Out of Scope / Acceptance Criteria / Files to Inspect First / Expected Files to Change / Risks / Test Plan / Rollback Plan / Git Context / Notion Page / Implementation Plan / Result / Test Result / Review Notes).

### 검증

- `pnpm typecheck` ✓
- `pnpm build` ✓
- `pnpm smoke` ✓ — 8 케이스 (`--help` / `init --dry-run` / `init --dry-run --git --initial-commit` / `status` / `task generate --dry-run` / `notion init --dry-run` / `github status` / `github init --dry-run --connect goodtek/vibeops`) 모두 회귀 없음.
- `pnpm publish --dry-run --access public --no-git-checks` ✓ exit 0. 출력 헤더에 `name: @goodtekxyz/vibeops`, `version: 0.2.0`, `total files: 93`, `Publishing to https://registry.npmjs.org/ with tag latest and public access (dry-run)` 확인. 실제 npm publish 는 수행하지 않음.
- 회귀 grep:
  - `node dist/cli.js --help` 출력에서 한국어 글자 0건.
  - `README.md` 내 `BYOBrowser` / `MVP 1` / `MVP 2` / `MVP 3` / `MVP 4` 0건. `support@goodtek.xyz` 1회, `hello@goodtek.xyz` 1회 확인.
  - `CHANGELOG.md` 내 `MVP 1` / `MVP 2` / `MVP 3` / `MVP 4` 0건.
  - `src/cli.ts` 내 한국어 글자 0건.
  - `src/types/config.ts` 내 `BYOBrowser` 0건.
  - `templates/.vibeops/agents/planner.md` 내 `BYOBrowser` 0건.

### 남은 위험 요소

- 실제 `npm publish` 는 본 TASK 범위 밖. 사용자가 직접 `pnpm publish --access public` (2FA 입력) 으로 수동 수행해야 함.
- 템플릿 markdown 본문(`templates/AGENTS.md`, `templates/.cursor/rules/*.mdc`, `templates/docs/project/**`) 의 한국어 → 영어 전면 번역은 별도 follow-up TASK 후보. 새 프로젝트 사용자가 한국어 markdown 을 받아도 CLI 동작은 정상이지만 글로벌 사용자 경험을 위해 정리 권장.
- `docs/project/` 하위 vibeops 자체 설계 문서는 npm 패키지에 포함되지 않으므로 (`package.json#files` 에 `docs/` 없음) historical record 로 유지. 최신 사실은 `03-current-state.md` 가 책임.
- `MVP Phase` Notion property 이름은 호환성 때문에 유지. 향후 변경하려면 사용자의 기존 Notion DB schema 도 같이 마이그레이션 필요.
- 기존 unscoped `vibeops` 패키지는 npm 에 publish 된 적 없으므로 (TASK-012 는 dry-run 만) 사용자 마이그레이션 부담 0.

## TASK-018 · Template English localization

TASK-017 직후 발견된 후속 정리. TASK-017 은 CLI / README / CHANGELOG 까지만 영문화했지만, npm tarball 에 동봉되는 `templates/` 본문은 여전히 한국어 — `vibeops init` 으로 새 프로젝트를 시작한 글로벌 사용자가 받는 첫 markdown 골격이 모두 한국어로 깔리던 문제를 해결. 본 TASK 의 결과로 `npm i -g @goodtekxyz/vibeops` 후 init 한 사용자의 워크스페이스에 한국어 글자 0건.

### 변경 사항

- `templates/AGENTS.md` — 8 섹션 (Purpose / Required reading / Source of truth / TASK-driven rules / Agent roles / Forbidden / Cursor rule files / Completion report / VibeOps metadata) 영문 번역. placeholder `{{PROJECT_NAME}}` / `{{VIBEOPS_VERSION}}` / `{{CREATED_AT}}` 그대로.
- `templates/.cursor/rules/00-project-governance.mdc` ~ `04-docs-update.mdc` 5 파일 — frontmatter (`description` / `alwaysApply`) 보존, 본문은 Source of truth / One TASK at a time / MVP scope / Refactors / Branch policy / Commit messages / Safety / Docs Update 8 섹션 영문 통일.
- `templates/.vibeops/agents/*.md` 8 파일 (`architect`, `builder`, `docs`, `orchestrator`, `planner`, `recovery`, `reviewer`, `tester`) — `Role / Inputs / Output Format / Rules / Forbidden` 5 섹션 모두 영문. planner.md 의 Acme Automator 예시는 TASK-017 에서 이미 영문이라 그대로.
- `templates/.vibeops/prompts/*.md` 6 파일 (`create-plan`, `generate-tasks`, `implement-task`, `review-task`, `rollback`, `start-project`) — Cursor 채팅창에 그대로 붙여 넣을 영문 markdown 로 재작성. frontmatter `placeholders` 그대로. `generate-tasks.md` 의 18-section 표준에 `Result` / `Test Result` body 를 `(not yet)` 으로 통일.
- `templates/.vibeops/workflows/*.md` 4 파일 (`notion-sync`, `project-start`, `rollback`, `task-lifecycle`) — 표 / 코드블록 / 명령 그대로, 산문만 영문 번역. `notion-sync.md` 의 NOTION_TOKEN 단일 secret + legacy `NOTION_API_KEY` / `NOTION_PROJECT_DB` / `NOTION_TASK_DB` deprecation 한 줄을 영문으로 유지.
- `templates/docs/project/*.md` 10 파일 (`00-overview` ~ `09-deployment`) — 모든 slot 주석 / 표 / 예시 영문. `08-env.md` 의 NOTION_TOKEN 표 + legacy env deprecation 영문 통일.
- `templates/docs/tasks/TASK-000-template.md` — 18-section 스켈레톤 영문. `## Result` / `## Test Result` body 가 `(not yet)` 로 통일되어 `vibeops task done` 의 PLACEHOLDER_RE 가 영문 / legacy 한국어 양쪽 모두 정상 인식 (TASK-017 에서 확장한 정책 활용).
- `templates/docs/logs/README.md` — 일일 로그 형식 표준 (`### Decision summary` / `### Changed files` / `### Verification` / `### Next work`) 영문 정리.
- vibeops 저장소 자신의 self-installed 미러 sync (18 파일, `.vibeops/agents/*.md` 8 + `.vibeops/prompts/*.md` 6 + `.vibeops/workflows/*.md` 4) — `cp templates/... .vibeops/...` 로 1:1 sync. `.vibeops/agents/planner.md` 는 TASK-017 에서 한 번 sync 했지만 다른 미러는 본 TASK 에서 처음 sync.
- `docs/project/03-current-state.md` — 현재 단계 줄에 "Template English localization" 추가, TASK 범위 `TASK-001 ~ TASK-018` 로 확장, "템플릿 콘텐츠 (36개)" 표 항목 영문화 정책 명시.
- `docs/tasks/TASK-018-template-english-localization.md` — 새 18-section TASK 파일 (Status=Review).

### 검증

- `pnpm typecheck` ✓ / `pnpm build` ✓ / `pnpm smoke` ✓ — 8 스모크 케이스 모두 회귀 없음. 본 TASK 의 변경은 markdown 텍스트뿐이라 TS 빌드 영향 0.
- `templates/**` 한국어 글자 0건. `.vibeops/**` (자체 미러) 한국어 글자 0건. `README.md` / `CHANGELOG.md` / `package.json` 한국어 0건.
- `src/**` 잔여 한국어: `src/lib/task.ts` 와 `src/lib/task-summary.ts` 의 legacy placeholder regex (예: `미수행`, `미정`, `채워라`) 만 — 의도된 backward compatibility (TASK-017 정책 유지). 영문 placeholder `(not yet)` / `(unassigned)` 도 같이 인식.
- Sandbox 검증 (`/var/folders/.../vibeops-task018-XXXX.TljdO0KFFI`):
  - `node dist/cli.js init --cwd <sandbox> --name "acme-automator" --git --initial-commit` exit 0. 39 created, 0 overwritten, 0 skipped. `git init` + default branch `main` + initial commit 정상.
  - `find <sandbox> -type f -not -path "*/.git/*" | wc -l` = 39. `.cursor/rules/*` 5 + `.vibeops/agents/*` 8 + `.vibeops/prompts/*` 6 + `.vibeops/workflows/*` 4 + `docs/project/*` 10 + `docs/tasks/TASK-000-template.md` 1 + `docs/logs/README.md` 1 + `AGENTS.md` + `.vibeops.env.example` + `.vibeops.json` + `.gitignore` = 39.
  - `grep -RP '[\\x{ac00}-\\x{d7a3}]' <sandbox>` 0건 — 사용자 워크스페이스 전체에 한국어 0 글자.
- `pnpm publish --dry-run --access public --no-git-checks` ✓ exit 0. `name: @goodtekxyz/vibeops`, `version: 0.2.0`, `total files: 93`, `package size: 122.9 kB` (TASK-017 대비 약 2.4 kB 감소 — 영문 번역의 자연스러운 결과), `unpacked size: 476.4 kB`, `Publishing to https://registry.npmjs.org/ with tag latest and public access (dry-run)`. tarball 에 `dist/`, `templates/`, `README.md`, `LICENSE`, `CHANGELOG.md` 모두 정상 포함.

### 남은 위험 요소

- vibeops 저장소 자신의 `AGENTS.md` / `.cursor/rules/**` / `docs/project/**` / `docs/tasks/TASK-001..017-*.md` / `docs/logs/2026-05-{11,12}.md` 의 한국어는 historical record 로 의도적 유지. 사용자 정책 "Do not remove Korean from historical docs/tasks unless those files are shipped to npm" 준수. npm 패키지에는 포함되지 않음.
- `src/lib/task.ts` / `src/lib/task-summary.ts` 의 legacy Korean placeholder regex 도 그대로 유지. 향후 정리하려면 별도 TASK 로 분리.
- 한국어 사용자가 영문 placeholder (`(not yet)` / `(unassigned)`) 가 익숙하지 않을 수 있음 — 본문은 자유롭게 한국어로 채울 수 있고 placeholder · section heading 만 영문 유지. `task done` validator 가 영문 / 한국어 placeholder 둘 다 인식하므로 회귀 없음.
- 실제 `npm publish` 는 본 TASK 범위 밖. 사용자가 직접 `pnpm publish --access public` (2FA 입력) 으로 수동 수행.
- 향후 vibeops 자체 설계 문서 영문화를 원하면 별도 TASK 후보. 현재 historical record 로 유지.
