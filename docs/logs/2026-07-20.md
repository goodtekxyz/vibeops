# 2026-07-20

## TASK-019 · task add integration sync UX

- Preflight integration sync before writing TASK md; classify dirty/ahead/diverged with fix commands.
- Resume incomplete In Progress (no task branch) on next `task add`.
- Shared helper used by `task add`, `task start`, and `pull`.
- Verification: `pnpm smoke` + `tests/git-integration-sync.test.mjs` pass.
- Version **2.5.1** prepared for npm publish.
