export function agentPromptCommand(name: string, taskId: string): void {
  console.log(`[vibeops] not implemented yet: agent prompt ${name} ${taskId}`);
  console.log("  → see docs/tasks/TASK-005-agent-commands.md");
}
