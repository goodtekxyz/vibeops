export function taskRollbackCommand(taskId: string): void {
  console.log(`[vibeops] not implemented yet: task rollback ${taskId}`);
  console.log("  (read-only stub — destructive actions require --confirm in TASK-009)");
  console.log("  → see docs/tasks/TASK-009-rollback-safety.md");
}
